#!/usr/bin/env ruby

require "bundler/setup"
require "parse4plex"

args = ARGV

performParse = false

if args[0] == '--parse'
  args.shift
  performParse = true
end

path = ''
args.each do |arg|
  path = arg
end

Parse4Plex.parse(path, performParse)
